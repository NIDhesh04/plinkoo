services:
  - type: web
    name: plinkoo-backend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    runtime: node
    branch: main
    region: oregon
    autoDeploy: true
